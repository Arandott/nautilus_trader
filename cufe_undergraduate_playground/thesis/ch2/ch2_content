# 第二章 相关技术与理论基础

## 2.1 L2订单簿与本地维护问题定义

订单簿是连续竞价市场中描述买卖双方报价状态的基础结构。其作用在于按照一定规则组织不同价格上的挂单信息，并为行情展示、撮合处理、交易决策和策略研究提供状态依据。根据市场数据粒度的不同，订单簿信息通常可以分为 L1、L2 和 L3 三个层次。L1 主要反映最优买价与最优卖价等最浅层盘口信息；L2 在此基础上进一步给出多个价格档位上的聚合数量；L3 则进一步细化到订单级别，能够描述同一价位内部更具体的委托构成。三者相比，L2 在信息丰富度与维护复杂度之间形成了较为均衡的层次，因此在本地行情维护、回放研究和低延迟交易支持中具有较高的应用价值。

与这一分层对应的，还有按价格组织与按订单组织两种常见方式。前者通常称为 MBP，即以价格档位为核心组织单位，同一价位上的数量以聚合值形式表现；后者通常称为 MBO，即以订单为核心组织单位，能够进一步保留同价位内部的订单级状态。本文关注的对象是 `L2_MBP` 场景下的本地订单簿维护问题，因此维护目标并不是逐笔订单集合，而是买卖两侧按价格层级聚合后的盘口状态。换言之，本文研究的核心对象是价格档位聚合量的维护，而不是完整订单生命周期的重建。

在本文的问题设定中，交易所原始 L2 增量 `delta` 被视为本地维护的真实状态依据。系统的任务不是重新定义市场状态，而是在本地侧按照输入顺序持续接收并应用这些增量，从而保持本地簿面与外部盘口之间的对应关系。由于增量数据天然依赖前序状态，本地维护过程具有明显的顺序敏感性。如果更新顺序被打乱，局部价位状态就可能发生偏移，进而影响最优价、深度结构以及上层策略逻辑。因此，本地 L2 维护首先是一个有状态、顺序驱动的增量处理问题。

围绕这一问题设定，本地维护系统至少需要具备四类基础能力。第一，系统需要能够围绕价格档位完成新增、更新、删除和清空等状态变更，以支持连续到达的原始增量输入。第二，系统需要支持最优价、最优量和 top-N 深度等核心查询，因为这些信息是本地交易决策和盘口研究中最常用的输入。第三，系统需要具备回放与状态重建能力，以便在给定同一份增量序列时重复获得一致的状态演进结果。第四，系统需要为研究与交易逻辑持续提供统一的本地盘口状态基础，而不仅仅是一次性的计算结果。

需要说明的是，本文虽然面向低延迟交易场景，但问题边界并不扩展到重型数据库、复杂持久化引擎或独立 checkpoint 系统的设计。论文更关注的是：在 `L2_MBP` 输入约束下，如何通过合适的数据组织方式完成本地状态维护，并在正确性、吞吐、时延和内存占用之间取得合理平衡。正是这一边界设定，使第二章后续的讨论能够集中在订单簿状态表示、更新路径、查询路径以及验证与测量方法等与论文主线直接相关的技术基础之上。

总体而言，本文所讨论的 L2 订单簿本地维护问题，可以概括为以下过程：以交易所原始增量为输入，以本地聚合盘口状态为对象，以顺序更新和低开销查询为核心任务，并同时满足可回放、可验证以及可供上层研究与交易使用的工程要求。这一问题定义既界定了后文系统设计的关注范围，也为基线实现与三种特化结构的引出提供了统一前提。

## 2.2 原生订单簿实现机制与基线技术基础

在通用市场数据处理系统中，订单簿通常不仅服务于单一数据层次，而需要兼容不同簿类型、不同操作语义以及更广泛的应用场景。因此，原生订单簿实现往往会采用较为通用的抽象体系，以便在不同输入条件和业务需求之间保持统一接口与一致行为。本文后续采用的基线实现为原生 `OrderBook(BookType::L2_MBP)`，它并不是临时构造的实验对象，而是建立在通用订单簿抽象之上的既有实现，因此可以作为后续分析与比较的工程起点。

从外部表现看，`OrderBook(BookType::L2_MBP)` 已经能够面向 L2 场景工作，能够围绕价位组织状态，并支持最优价访问和有序深度读取。这说明基线实现本身并不脱离论文的研究目标，相反，它已经具备 L2 场景所需的基本能力，也正因如此才能成为合理的对照对象。本文后续讨论的专门化空间，并不是对基线实现设计价值的否定，而是在承认其通用性与工程完整性的基础上，进一步思考其在纯 L2 本地维护问题中的优化余地。

进一步看，通用订单簿之所以会保留较多抽象层，是因为它不仅要表达“某一价位上有多少量”，还需要兼容更一般的订单簿语义与内部组织方式。对于这类实现而言，围绕价位、内部容器和辅助状态建立统一表述，有助于在不同簿类型之间复用机制，降低整体接口复杂度，并维持实现上的一致性。也就是说，基线实现内部所保留的多层结构，并不是无意义的负担，而是在可扩展性、可维护性和语义统一性约束下形成的设计结果。

然而，当问题进一步收缩到“面向低延迟交易的本地 L2 维护”时，这种通用抽象也会自然暴露出继续精简的空间。对于纯 `L2_MBP` 场景，本地维护对象本质上是按价格组织的聚合数量，而不是完整订单集合。若系统内部仍保留更多面向一般场景的组织层次、辅助状态和中间转换语义，就意味着在部分更新与查询路径上仍需要承担额外的结构跳转和容器管理成本。这里所说的成本并不是对基线实现的负面评价，而是在更窄问题边界下，对“通用性”与“专门化”之间工程权衡的进一步观察。

也正因为如此，基线实现在本文中承担着双重作用。一方面，它是系统实现与正确性比较的技术基础。后续统一接口、验证逻辑和对照实验，都需要有一个语义完整且行为明确的起点，而原生 `OrderBook(BookType::L2_MBP)` 正好提供了这一基础。另一方面，它也是结构专门化思考的参照对象。只有在理解基线实现为何保留较多通用语义之后，后续对 `L2TreeBook`、`L2VecBook` 和 `L2GridBook` 的设计动机才会更加清晰。

因此，本节的核心结论并不是“原生实现不适合 L2 场景”，而是“原生实现已经提供了有效基础，但在纯 L2 低延迟维护问题中仍存在进一步专门化的合理动机”。这一判断为后续第三章的系统总体设计提供了技术起点，也为第四章对基线实现集成与三类特化结构实现的说明奠定了概念基础。

## 2.3 低延迟本地维护涉及的关键技术

面向低延迟交易的本地 L2 订单簿维护，并不是单纯的数据存取问题，而是一个同时受状态组织方式、更新路径长度、内存访问行为以及验证方法影响的综合工程问题。由于原始 `delta` 持续到达，本地系统既要能够快速吸收增量，又要在尽量低的额外开销下提供稳定查询。因此，围绕 L2 维护所涉及的关键技术，至少可以从动态有序组织、连续内存布局、离散化索引、回放重建机制以及正确性与性能评估方法几个方面来理解。

首先，从数据组织角度看，L2 订单簿天然具有“按价格有序”的要求。买卖两侧都需要维持价位顺序，以支持最优价访问和前若干档深度读取；与此同时，增量输入又会不断带来价位的新增、修改与删除。因此，能够同时兼顾顺序维护和动态更新的有序结构，构成了低延迟本地维护中的一条自然路线。对于本文而言，这也解释了树结构路线的技术动机。若系统能够在保留有序性的同时，去除 L2 场景并不需要的订单级中间语义，就有可能在保持动态更新稳定性的前提下，进一步压缩维护路径。

其次，低延迟系统的实际表现不仅取决于逻辑步骤数量，也显著受内存访问方式影响。对于 L2 订单簿而言，许多高频查询集中在最优价附近以及前若干档深度信息上，具有较强的局部访问特征。基于这一点，连续内存布局构成了另一条自然路线。向量式结构的意义并不只是“换一个容器”，而在于利用更紧凑的状态表示和更直接的遍历方式，探索局部查询与连续深度读取的优化空间。对于本文而言，这正是 `L2VecBook` 被提出的理论基础。

在此基础上，如果进一步利用市场价格以 tick 为基本单位离散变化这一特征，还可以从“有序比较”进一步转向“索引映射”。在一定条件下，价格可以通过离散化映射到更直接的索引空间，从而减少一般性比较或查找路径带来的额外成本。这便构成了 grid 路线的技术动机。需要指出的是，这里的重点并不在于某一种具体实现细节，而在于一种更激进的专门化思路，即尽量把“按价格查找”转化为“按离散索引访问”，从而探索更短路径和更强特化所可能带来的收益。`L2GridBook` 正是在这种思路下被引出的。

需要说明的是，树、向量和 grid 三条路线并不是相互否定的关系，而是分别对应低延迟优化中的不同关注点。树结构强调在动态更新过程中保留顺序稳定性，适合从通用有序组织向 L2 专门化平滑过渡；向量结构强调连续布局与局部访问效率，更适合读取密集型场景；grid 结构则进一步利用价格离散性，探索更短的维护路径和更强的结构特化。三条路线之所以都成立，根本原因在于 L2 本地维护同时包含“有序”“动态”“高频查询”“局部访问”和“价格离散化”这几个约束维度，不同结构只是从不同角度回应这些约束。

除数据结构本身外，增量回放与状态重建也是低延迟本地维护中的关键技术。由于原始 `delta` 被视为状态演进的依据，本地系统不仅要在单次运行中保持正确，还应能够在给定相同输入序列时重复得到一致的状态结果。这使回放机制具有双重意义：一方面，它是调试和研究的重要支撑；另一方面，它也是不同实现得以在统一条件下进行比较的必要前提。只有在可重复的输入驱动下，结构差异所带来的性能与行为差异才具有可分析性。

与回放同等重要的，是正确性验证机制。低延迟系统虽然强调吞吐和时延，但对于订单簿维护而言，任何性能优化都必须建立在状态可信的前提之上。若某一结构在价位删除、数量归零、最优价切换或连续增量作用下出现偏差，即使局部性能更优，也无法为上层交易与研究逻辑提供可靠基础。因此，正确性验证不是实现完成后的附加项，而是与接口抽象、结构设计和回放机制同样重要的组成部分。

在性能分析方法上，微基准测试与真实工作负载也具有互补意义。微基准测试的作用在于把复杂系统行为拆解为更小粒度的局部路径，便于观察某一类更新或查询操作在不同结构上的开销差异；真实回放工作负载则能够反映连续增量流下的整体吞吐、查询成本和内存表现。前者有助于理解局部行为，后者更接近系统真实使用状态。对于本文而言，只有将两者结合起来，才能对不同结构路线形成较完整的工程判断。

综上所述，本节的作用不是提前判断哪一种结构最终更优，而是为后续章节建立必要的技术前提。树结构路线说明了在保留有序更新能力的同时压缩通用语义的可能性；向量路线说明了连续布局与局部访问特征所带来的现实吸引力；grid 路线说明了价格离散化和索引化访问能够为更激进的专门化提供方向。与此同时，回放重建、正确性验证以及微基准测试与真实工作负载相结合的分析方法，又共同构成了后续系统设计、实现和测试验证的基础条件。

## 2.4 本章小结

本章围绕“面向低延迟交易的本地 L2 订单簿维护系统”所涉及的核心概念、技术基础和问题边界进行了梳理。首先，本文明确了 L1、L2、L3 以及 MBP、MBO 之间的区别，指出本论文处理的对象是以价格档位聚合量为核心的 `L2_MBP` 本地状态，并将原始 L2 增量 `delta` 界定为本地维护的真实输入来源。在此基础上，进一步说明了本地维护系统需要承担顺序增量更新、最优价与 top-N 查询、回放与状态重建以及为研究和交易提供状态支持等基本任务。

其次，本章对原生 `OrderBook(BookType::L2_MBP)` 的机制特征进行了概括，并将其定位为后续系统实现中的基线技术基础。通过对通用订单簿抽象与专门化空间之间关系的讨论，可以看出基线实现在 L2 场景下已经具备有效能力，同时也说明了在纯 L2 低延迟维护问题中继续开展结构特化的合理性。

最后，本章从动态有序组织、连续内存布局、离散化索引、增量回放、正确性验证以及性能分析方法等角度，对后续技术路线进行了铺垫。由此，`L2TreeBook`、`L2VecBook` 和 `L2GridBook` 三条路线得以在统一问题背景下被自然引出，并分别对应不同的低延迟优化思路。下一章将在上述理论基础上，进一步展开系统需求分析与总体设计。

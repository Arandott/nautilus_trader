## Nautilus Aurora Support Crate

This crate hosts reusable Rust components (fill, inventory/grid planning, risk helpers) shared across Aurora strategy bindings. It intentionally contains no PyO3 code; Python bindings live in `crates/pyo3`.

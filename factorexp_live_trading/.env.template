# ========================================
# FactorExp Live Trading - Environment Configuration
# ========================================
# 
# This template shows all available environment variables for the FactorExp Live Trading System.
# Copy this file to `.env` and configure with your actual values.
#
# IMPORTANT: The system now uses StrategyConfig classes for most trading parameters.
# Environment variables are only used for API keys, trading mode, and system settings.

# ========================================
# 🔐 CRITICAL: API CREDENTIALS (REQUIRED)
# ========================================
# Get these from your Binance account:
# 1. https://www.binance.com/en/my/settings/api-management  (live)
# 2. https://testnet.binancefuture.com/                  (testnet futures)
#
# Provide BOTH live and testnet keys if you plan to switch via TRADING_MODE.

# Live trading credentials
BINANCE_API_KEY=
BINANCE_API_SECRET=

# Testnet credentials (used automatically when TRADING_MODE=testnet)
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_API_SECRET=

# ========================================
# 🎯 CORE TRADING CONFIGURATION
# ========================================

# Trading mode: 'testnet' (default) or 'live'
TRADING_MODE=testnet

# Comma separated instruments to trade (InstrumentId strings)
# Example: TRADING_INSTRUMENTS=BTCUSDT-PERP.BINANCE,ETHUSDT-PERP.BINANCE
TRADING_INSTRUMENTS=

# Optional account size in USD. Set to a number (e.g. 200, 500) to enable
# small-account safeguards; leave blank or 'Standard' for default behaviour.
ACCOUNT_SIZE_USD=

# ========================================
# 🧮 FACTOREXP STRATEGY OVERRIDES (OPTIONAL)
# ========================================

FACTOREXP_CONFIG_PATH=../factorexp_backtest/configs/factors.yaml
FACTOREXP_FACTOR_ID=vwap_return_std
FACTOREXP_ZSCORE_PERIOD=5760
FACTOREXP_CLIP_MIN=-2.0
FACTOREXP_CLIP_MAX=2.0
FACTOREXP_MIN_SIGNAL=0.05

# ========================================
# ⚙️ SYSTEM & LOGGING SETTINGS (OPTIONAL)
# ========================================

# Console log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# File log level (see config/trading_config.py)
LOG_LEVEL_FILE=DEBUG

# Output structured JSON logs
ENABLE_STRUCTURED_LOGGING=true

# Portfolio monitor cadence (seconds)
PORTFOLIO_UPDATE_INTERVAL_SEC=5

# Enable portfolio change alerts
ENABLE_PORTFOLIO_ALERTS=true

# Trigger alert when P&L moves beyond this fraction (e.g. 0.02 = 2%)
ALERT_THRESHOLD_PNL_PCT=0.02

# ========================================
# 🔔 ALERTING & NOTIFICATIONS (OPTIONAL)
# ========================================

# Email alerts (requires SMTP settings below)
ENABLE_EMAIL_ALERTS=false

# SMTP credentials (only used when ENABLE_EMAIL_ALERTS=true)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Webhook alerts (Discord, Slack, etc.)
ENABLE_WEBHOOK_ALERTS=false
WEBHOOK_URL=

# ========================================
# 🧪 TEST / CI SETTINGS (OPTIONAL)
# ========================================

# Enable live integration tests (used in test suites)
NT_ENABLE_LIVE_TESTS=false

# ========================================
# 🧠 ADVANCED / LEGACY PARAMETERS (OPTIONAL)
# ========================================
# Only set these if you rely on legacy hooks in SecureConfigManager.

MAX_POSITION_SIZE_BTC=
MAX_POSITION_SIZE_ETH=
RISK_LIMIT_DAILY_USD=
MAX_DRAWDOWN_PCT=

# ========================================
# 📊 STRATEGY CONFIGURATION NOTES
# ========================================
#
# 🎯 IMPORTANT: Most trading parameters are now configured via StrategyConfig classes!
# 
# The following parameters are NO LONGER configured via environment variables:
# ❌ MAX_POSITION_SIZE_BTC (now: position_risk_pct in StrategyConfig)
# ❌ MAX_DAILY_LOSS_USD (now: max_daily_loss_usd in StrategyConfig)  
# ❌ MAX_DRAWDOWN_PCT (now: max_drawdown_pct in StrategyConfig)
# ❌ STOP_LOSS_PCT (now: stop_loss_pct in StrategyConfig)
# ❌ TAKE_PROFIT_PCT (now: take_profit_pct in StrategyConfig)
#
# These are now configured using type-safe StrategyConfig classes:
# 
# ✅ Standard Account Configuration:
#    - max_account_usage_pct: 80%
#    - position_risk_pct: 2%  
#    - stop_loss_pct: 1.5%
#    - max_daily_trades: 20
#
# ✅ Small Account Configuration (≤$500):
#    - max_account_usage_pct: 60%
#    - position_risk_pct: 1.5%
#    - stop_loss_pct: 1.2%  
#    - max_daily_trades: 10
#
# 📖 See examples/small_account_example.py for configuration examples
# 📖 See config/strategy_config.py for all available parameters

# ========================================
# 🚀 QUICK START GUIDE
# ========================================
#
# 1. Copy this file: cp .env.template .env
# 2. Configure your Binance API credentials above
# 3. Set TRADING_MODE=testnet for testing
# 4. Optionally set your ACCOUNT_SIZE_USD for optimization
# 5. Run: python start_trading.py
#
# 🔒 Security Tips:
# - Never commit your .env file to version control
# - Use testnet first to validate your setup
# - Keep your API keys secure and never share them
# - Use read-only API keys when possible for monitoring

# ========================================
# 📚 ADDITIONAL RESOURCES
# ========================================
#
# 📖 Full Documentation: DEPLOYMENT_GUIDE.md
# 💰 Small Account Guide: examples/small_account_example.py
# 🔧 Configuration Reference: config/strategy_config.py
# 🚀 Startup Script: start_trading.py
# 📊 Architecture: Uses official Nautilus Trader StrategyConfig pattern

# GPT-5 Pro Prompt for Chapter 2

你现在扮演一位中文计算机/信息系统类本科毕业论文正文写手，请为我的论文撰写**第二章 相关技术与理论基础**初稿。

## 一、你的任务

请围绕以下论文题目，直接写出一版**可用于毕业论文正文**的第二章完整初稿，而不是提纲、摘要或写作建议。

论文题目：

- `面向低延迟交易的本地L2订单簿维护系统`

本章必须使用以下固定结构：

- `2.1 L2订单簿与本地维护问题定义`
- `2.2 原生订单簿实现机制与baseline技术基础`
- `2.3 低延迟本地维护涉及的关键技术`
- `2.4 本章小结`

请输出为正式中文学术写作风格，**以段落为主**，不是要点罗列。

你**不能访问我的仓库、代码、实验目录或本地文件**，因此本 prompt 已经提供你撰写第二章所需的核心背景。请不要假设你还能读取额外上下文，也不要因为拿不到仓库而缩写内容。

## 二、整篇论文的结构安排（必须参考）

为了让第二章和全文结构保持一致，你必须按下面这套章法来理解本论文。

本论文采用六章结构：

- 第一章 绪论
  - 1.1 课题背景
  - 1.2 国内外研究现状
  - 1.3 研究意义、研究内容与研究目标
  - 1.4 论文结构
- 第二章 相关技术与理论基础
  - 2.1 L2订单簿与本地维护问题定义
  - 2.2 原生订单簿实现机制与baseline技术基础
  - 2.3 低延迟本地维护涉及的关键技术
  - 2.4 本章小结
- 第三章 系统需求分析与总体设计
  - 3.1 系统需求分析
  - 3.2 系统总体设计
  - 3.3 功能模块设计
  - 3.4 系统设计优劣分析
  - 3.5 设计中的制约因素
  - 3.6 成本或工作量估算
  - 3.7 本章小结
- 第四章 系统实现
  - 4.1 实现基础
  - 4.2 统一接口与公共机制实现
  - 4.3 Baseline集成与对照实现
  - 4.4 `L2TreeBook` 实现
  - 4.5 `L2VecBook` 实现
  - 4.6 `L2GridBook` 实现
  - 4.7 正确性验证模块实现
  - 4.8 Benchmark与replay测量模块实现
  - 4.9 结果处理与面向写作的分析支撑实现
  - 4.10 本章小结
- 第五章 测试与结果分析
  - 5.1 测试环境
  - 5.2 测试设计、测试结果与结果分析
  - 5.3 本章小结
- 第六章 总结与展望

第二章在整篇论文中的角色是：

- 为第三章“系统需求分析与总体设计”提供概念边界和技术铺垫
- 为第四章“系统实现”提供术语、机制和路线动机
- 为第五章“测试与结果分析”提供理解性能差异所需的前置背景

所以第二章应该写成：

- 理论基础
- 技术基础
- 问题定义与路线铺垫

而不是：

- 具体实现说明
- 结果分析
- 对某种结构的提前裁决

## 三、你必须掌握的系统背景包

下面是你写第二章时必须依赖的自包含背景信息。

### 1. 论文研究对象

本论文研究的是一个**面向低延迟交易的本地L2订单簿维护系统**。

这意味着系统需要面向以下目标：

- 在原始市场增量数据持续到达时，快速维护本地L2状态
- 支持低开销查询
- 为本地研究、回放和交易决策提供盘口状态基础
- 在正确性、吞吐、时延和内存占用之间寻找工程平衡

### 2. 输入、对象与任务边界

系统围绕交易所原始L2增量 `delta` 数据展开。

原始 `delta` 被视为 `source of truth`。系统在本地侧要完成的核心任务包括：

- 顺序接收与应用增量更新
- 维护买卖两侧价位状态
- 提供 `best bid/ask` 与 `top-N` 等核心查询
- 支持 replay / 状态重建
- 为策略研究或交易侧逻辑提供本地订单簿状态基础

本论文当前主线不包括：

- 自定义数据库
- 重型存储引擎设计
- 复杂checkpoint与恢复机制作为主要创新点

### 3. baseline 与三条特化路线

当前系统中的对照实现是：

- 原生 `OrderBook(BookType::L2_MBP)`

在此基础上，论文围绕 `L2_MBP` 设计了三种特化结构：

- `L2TreeBook`
- `L2VecBook`
- `L2GridBook`

这三条路线的技术动机分别是：

- `L2TreeBook`
  - 在保留有序性和稳定动态插删的前提下，去除L2不需要的订单级抽象
- `L2VecBook`
  - 利用连续内存布局与查询局部性，探索更紧凑的访问路径
- `L2GridBook`
  - 利用tick离散化与更专门化的索引组织，探索进一步削减结构路径开销

### 4. baseline 的已知技术特征

第二章可以使用以下技术事实作为基础：

- baseline 仍建立在通用订单簿抽象之上
- 虽然面向 `L2_MBP` 运行，但内部并非纯粹的 `price -> aggregate quantity` 结构
- 它已经具备按价位有序组织和相对直接的 best price 查询能力
- 但在纯L2场景中仍保留了额外的通用语义和容器层次

这里的写法必须是：

- “通用设计在特定场景下仍有进一步专门化空间”

而不能写成：

- “baseline 本身是错误设计”

### 5. 当前论文的阶段性状态

本论文已经建立了如下研究闭环：

- baseline 路径已被建模
- 三种特化结构都已进入实现与验证体系
- correctness 验证机制已建立
- microbench 与 replay workload 验证框架已建立

但第二章不能把这些写成实验结果章节，最多只能把它们写成：

- 后文将基于这些技术路线展开系统设计、实现与测试验证

## 四、必须严格遵守的事实边界

请只在下面这些已知事实范围内组织内容，不得发明实现、结果、引用或技术细节。

### 1. 系统主题与边界

- 本论文关注的是**面向低延迟交易的本地L2订单簿维护系统**。
- 本论文的重点是：
  - 如何在原始L2增量 `delta` 输入下完成本地状态维护
  - 如何围绕 `L2_MBP` 做结构专门化
  - 如何在正确性、吞吐、时延和内存之间寻找工程平衡
- 本论文当前主线**不是**重做存储引擎、数据库或复杂checkpoint系统。
- 原始交易所 `delta` 被视为 `source of truth`。

### 2. baseline 与三种特化设计

- baseline 是原生 `OrderBook(BookType::L2_MBP)`。
- 系统中还实现了三种面向 `L2_MBP` 的结构：
  - `L2TreeBook`
  - `L2VecBook`
  - `L2GridBook`
- 第二章的任务是为第三章和第四章的系统设计与实现铺设技术基础，而不是在本章分析实验结果。
- 三种结构都要被写成“有明确技术动机的设计路线”。

### 3. baseline 已知的关键技术事实

下面这些是可以写入第二章的：

- 原生 `OrderBook(BookType::L2_MBP)` 仍建立在通用订单簿抽象之上。
- L2 外部输入在内部会经历 price 到 synthetic `order_id` 的映射语义。
- baseline 中存在：
  - `BookLadder`
  - `BookLevel`
  - `cache`
  - level 内 order 容器等通用结构
- baseline 在 L2 场景下已经具备按价位组织和有序查询能力。
- 但对纯 `L2_MBP` 场景来说，它仍保留了额外的通用抽象层。

注意：

- 请把这些写成“合理的通用设计在特定场景下仍有专门化空间”，而不是“baseline 很差”。

### 4. 三种特化结构的技术动机

以下动机可以写，但不要展开成第四章实现细节：

`L2TreeBook`：

- 在保留有序性和动态插删稳定性的前提下，去掉L2不需要的订单级语义。

`L2VecBook`：

- 强调连续内存布局与 query locality，探索在本地L2维护中是否能进一步改善查询与访问局部性。

`L2GridBook`：

- 利用 tick 离散化和更激进的索引结构，探索是否能进一步削减树比较与路径开销。

### 5. 不允许出现的写法

请不要出现以下问题：

- 不要在第二章提前写具体 benchmark 数字
- 不要在第二章直接下结论说某设计“失败”或“最优”
- 不要虚构算法复杂度证明、文献来源、工业落地情况
- 不要把第四章的具体实现代码细节大段搬进第二章
- 不要把第五章的测试分析提前写进来

## 五、本章写作目标

第二章要完成的事情是：

1. 定义论文中的核心概念与问题边界。
2. 交代 baseline 所代表的技术基础与改进起点。
3. 说明为什么会自然地引出树、向量、grid 这三类路线。
4. 为第三章“系统需求分析与总体设计”和第四章“系统实现”做理论与技术铺垫。

## 六、各节具体写作要求

### 2.1 L2订单簿与本地维护问题定义

这一节应写清：

- `L1 / L2 / L3` 的区别
- `MBP` 与 `MBO` 的区别
- 本论文中的L2对象本质上是 `price level aggregate`
- 本地维护系统面对的是顺序到达的原始增量数据
- 本地维护系统为什么需要支持：
  - 增量更新
  - best bid / ask 查询
  - top-N 查询
  - replay / 重建
  - 面向交易与研究的状态提供

写法要求：

- 以问题定义为主
- 不要直接跳到某个具体实现

### 2.2 原生订单簿实现机制与baseline技术基础

这一节要把 baseline 作为“技术基础”和“对照起点”写清楚。

建议组织逻辑：

- 原生 `OrderBook(BookType::L2_MBP)` 的通用性背景
- 为什么通用系统会保留更多抽象层
- baseline 在 L2 场景下的技术特点：
  - 有序的 price level 组织
  - level 容器
  - synthetic `order_id`
  - cache
- 这些设计为什么在通用系统中合理
- 为什么在纯L2低延迟场景下仍值得进一步专门化

注意：

- 这一节要写得专业、克制，避免“批判式语气”

### 2.3 低延迟本地维护涉及的关键技术

这是第二章最重要的一节。

请围绕以下方向组织：

- 有序树与动态更新
- 连续内存布局与缓存局部性
- tick 离散化、索引映射、page/bitmap 思路
- 增量回放与状态重建
- correctness 验证的必要性
- microbench 与真实 workload 在低延迟系统分析中的不同作用

这一节的目标不是展开实现，而是建立后文的技术动机：

- 为什么树结构路线自然成立
- 为什么向量路线自然成立
- 为什么grid路线自然成立

请特别注意：

- 要把三种路线都写得有道理
- 不要在这里偷偷带入第五章的结果倾向

### 2.4 本章小结

请简洁总结：

- 第二章完成了概念界定、baseline 技术基础说明和关键技术路径铺垫
- 第三章将据此展开系统需求分析与总体设计

同时请体现：

- 第二章是为第三章和第四章服务的
- 本章没有对第五章结果作提前裁决

## 七、文风要求

请使用以下风格：

- 中文本科毕业论文风格
- 清楚、正式、逻辑强
- 不要口语化
- 不要写成技术博客
- 不要写成代码注释式说明
- 不要堆砌空话

## 八、引用与事实安全要求

第二章很容易出现“看起来专业但其实没来源”的问题，请严格遵守：

1. 如果没有我提供的可靠来源，不要虚构具体文献。
2. 如需引用位置，请只保留占位符，例如：
   - `[文献待补-1]`
   - `[文献待补-2]`
3. 不要虚构具体论文作者、会议、年份和结论。
4. 不要虚构性能数据或复杂度结论。
5. 对没有被我明确确认的地方，宁可写得审慎，也不要写得过满。

## 九、给你的额外写作提醒

- 第二章一定要把“为什么会自然引出三条路线”写出来。
- 你可以写“树结构、向量结构、grid结构各自对应不同的低延迟优化思路”，但不要在这里下实验结论。
- 对研究现状和技术背景的表述要审慎，缺文献时保留占位符。
- 第二章应在结尾自然过渡到第三章“系统需求分析与总体设计”。

## 十、输出格式要求

请直接输出：

1. `第二章 相关技术与理论基础`
2. `2.1 L2订单簿与本地维护问题定义`
3. `2.2 原生订单簿实现机制与baseline技术基础`
4. `2.3 低延迟本地维护涉及的关键技术`
5. `2.4 本章小结`

正文写完后，请额外附一个很短的附录段落，标题为：

- `【需作者后补的文献或事实点】`

这个附录里只需列出：

- 哪些位置建议补文献
- 哪些技术表述建议作者再核实

除了这个短附录，不要输出解释，不要输出提纲，不要输出写作说明。
